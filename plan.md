# План розробки Telegram-бота "Nova Poshta AI Waybill Generator"

## Опис проекту
Створення Telegram-бота на Python (aiogram 3.x), який за допомогою штучного інтелекту (OpenAI-сумісного API / Gemini) парсить неструктурований текст із реквізитами отримувача без регулярних виразів, формує експрес-накладні (ТТН як у відділення/поштомати, так і на адресну доставку кур'єром), забезпечує інтелектуальне об'єднання чернеток у реєстри (ScanSheet) на основі природномовних запитів та генерує штрих-коди Code128 для швидкого сканування на відділеннях Нової Пошти.

## Основні компоненти (v0.21.0)
1. [x] **AI Entity Extractor, Regex Healing & Smart Contextual Editing (`src/ai/`)**:
   - Повна ізоляція AI-провайдера за Telegram `user_id`.
   - Дворівневий механізм захисту та автолікування сутностей `heal_parsed_recipient_info` (гарантований парсинг навіть при збоях AI).
   - Ігнорування метаданих пересилання (`"Переслано від Vlad Martyniuk"`).
   - Повноцінний парсинг імен з ініціалами (`Мартинюк Є.В.`) та суржикових термінів (`почтомат`, `отделение`).
   - Повноцінне природномовне редагування будь-якої частини накладної (ПІБ, телефон, місто, вулиця/будинок, відділення/поштомат, опис, оцінка, наложка, платник).
   - Розпізнавання суми та виплати накладеного платежу (`cod_amount`, `cod_payment_type`).
   - Розпізнавання платника (`payer_type`) та типу вантажу (`cargo_type`).
   - Метод `filter_drafts_for_register` — прийом масиву активних чернеток у форматі JSON разом із промптом користувача.

2. [x] **Синхронізація та надійне видалення чернеток ТТН (v0.19.9 - v0.19.10)**:
   - Розпізнавання статусів видалених/неіснуючих накладних (`StatusCode 2`, `3`, `"Видалено"`) та відмов (`103`) у `get_documents_status`.
   - Автоматичне вилучення видалених ТТН із локальної бази `user_drafts.json` у `fetch_user_active_drafts`.
   - Очищення фільтрацією у `get_internet_document_list` для ігнорування `DeletionMark`/`Видалено`.
   - Видалення у `storage.py` як за `ref` GUID, так і за `int_doc_number`.

3. [x] **Генерація штрих-кодів для накладних та реєстрів (v0.20.0)**:
   - Інтерактивна кнопка `📱 Показати штрихкод` під кожною ТТН у чернетках, вихідних та вхідних посилках.
   - Генерація високоякісного Code128 штрихкоду для ТТН та реєстрів Нової Пошти.

4. [x] **Nova Poshta API Client & Address Delivery (`src/nova_poshta/`, `src/utils/`)**:
   - Пряме завантаження чернеток з кабінету НП через `InternetDocument/getInternetDocumentList`.
   - Інтелектуальний пошук вулиць через `Address/getStreet` з перестановками слів та ранжуванням результатів.
   - Реєстрація адрес отримувача через `Address/save`.
   - Створення накладних адресної доставки (`ServiceType: "WarehouseDoors"`).

5. [x] **Telegram Bot Interface & Keyboards (`src/bot/`)**:
   - Повне очищення активної сесії при підтвердженні створення ТТН або скасуванні (`clear_user_active_session`).
   - Збереження всіх змінених параметрів сесії (платник, вантаж, оцінка, наложка) при текстових доповненнях/редагуваннях.
   - Інтерактивна клавіатура вибору вулиці/провулку (`StreetSelectCallback`) при знаходженні декількох варіантів.
   - Компактна схема `WaybillActionCallback` (action, session_id), що унеможливлює ліміт 64 байти в Telegram.

6. [x] **Персональне збереження даних (`src/storage.py`)**:
   - Індивідуальне збереження налаштувань користувача в `data/user_settings.json` за Telegram `user_id`.
   - Автоматичне очищення відправлених чернеток (`purge_sent_drafts`) та застарілих/відправлених реєстрів (`purge_old_or_sent_scansheets`).

7. [x] **15-хвилинна сесія редагування, фіксація обраного міста та розпізнавання опису/оцінки (v0.21.0)**:
   - [x] Впровадження 15-хвилинного TTL (`SESSION_TIMEOUT_SECONDS = 900`) для активних сесій формування посилки з автоматичним очищенням при неактивності.
   - [x] Усунення зациклення вибору міста: фіксація обраного міста/відділення/вулиці в сесії без повторного запиту при подальших текстових змінах оцінки чи опису.
   - [x] Розширення `heal_parsed_recipient_info` регулярними виразами для автоматичного розпізнавання оцінки (`declared_value`), опису вантажу (`cargo_description`), наложки (`cod_amount`) та платника.
   - [x] Написання юніт-тестів для перевірки збереження сесії, тайм-ауту та оновлення полів.