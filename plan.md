# План розробки Telegram-бота "Nova Poshta AI Waybill Generator"

## Опис проекту
Створення Telegram-бота на Python (aiogram 3.x), який за допомогою штучного інтелекту (OpenAI-сумісного API / Gemini) парсить неструктурований текст із реквізитами отримувача без регулярних виразів, формує експрес-накладні (ТТН як у відділення/поштомати, так і на адресну доставку кур'єром), забезпечує інтелектуальне об'єднання чернеток у реєстри (ScanSheet) на основі природномовних запитів та генерує штрих-коди Code128 для швидкого сканування на відділеннях Нової Пошти.

## Основні компоненти (v0.24.10)
1. [x] **AI Entity Extractor, Regex Healing & Smart Contextual Editing (`src/ai/`)**:
   - Повна ізоляція AI-провайдера за Telegram `user_id`.
   - Дворівневий механізм захисту та автолікування сутностей `heal_parsed_recipient_info` (гарантований парсинг навіть при збоях AI).
   - Ігнорування метаданих пересилання (`"Переслано від Vlad Martyniuk"`).
   - Повноцінний парсинг імен з ініціалами (`Мартинюк Є.В.`) та суржикових термінів (`почтомат`, `отделение`).
   - Повноцінне природномовне редагування будь-якої частини накладної (ПІБ, телефон, місто, вулиця/будинок, відділення/поштомат, опис, оцінка, наложка, платник).
   - Розпізнавання суми та виплати накладеного платежу (`cod_amount`, `cod_payment_type`).
   - Розпізнавання платника (`payer_type`) та типу вантажу (`cargo_type`).
   - Метод `filter_drafts_for_register` — прийом масиву активних чернеток у форматі JSON разом із промптом користувача.

2. [x] **Синхронізація та надійне видалення чернеток ТТН (v0.19.9 - v0.19.10)**:
   - Розпізнавання статусів видалених/неіснуючих накладних (`StatusCode 2`, `3`, `"Видалено"`) та відмов (`103`) у `get_documents_status`.
   - Автоматичне вилучення видалених ТТН із локальної бази `user_drafts.json` у `fetch_user_active_drafts`.
   - Очищення фільтрацією у `get_internet_document_list` для ігнорування `DeletionMark`/`Видалено`.
   - Видалення у `storage.py` як за `ref` GUID, так і за `int_doc_number`.

3. [x] **Генерація штрих-кодів для накладних та реєстрів (v0.20.0)**:
   - Інтерактивна кнопка `📱 Показати штрихкод` під кожною ТТН у чернетках, вихідних та вхідних посилках.
   - Генерація високоякісного Code128 штрихкоду для ТТН та реєстрів Нової Пошти.

4. [x] **Nova Poshta API Client & Address Delivery (`src/nova_poshta/`, `src/utils/`)**:
   - Пряме завантаження чернеток з кабінету НП через `InternetDocument/getInternetDocumentList`.
   - Інтелектуальний пошук вулиць через `Address/getStreet` з перестановками слів та ранжуванням результатів.
   - Реєстрація адрес отримувача через `Address/save`.
   - Створення накладних адресної доставки (`ServiceType: "WarehouseDoors"`).

5. [x] **Telegram Bot Interface & Keyboards (`src/bot/`)**:
   - Повне очищення активної сесії при підтвердженні створення ТТН або скасуванні (`clear_user_active_session`).
   - Збереження всіх змінених параметрів сесії (платник, вантаж, оцінка, наложка) при текстових доповненнях/редагуваннях.
   - Інтерактивна клавіатура вибору вулиці/провулку (`StreetSelectCallback`) при знаходженні декількох варіантів.
   - Компактна схема `WaybillActionCallback` (action, session_id), що унеможливлює ліміт 64 байти в Telegram.

6. [x] **Персональне збереження даних (`src/storage.py`)**:
   - Індивідуальне збереження налаштувань користувача в `data/user_settings.json` за Telegram `user_id`.
   - Автоматичне очищення відправлених чернеток (`purge_sent_drafts`) та застарілих/відправлених реєстрів (`purge_old_or_sent_scansheets`).

7. [x] **15-хвилинна сесія редагування, фіксація обраного міста та розпізнавання опису/оцінки (v0.21.0)**:
   - [x] Впровадження 15-хвилинного TTL (`SESSION_TIMEOUT_SECONDS = 900`) для активних сесій формування посилки з автоматичним очищенням при неактивності.
   - [x] Усунення зациклення вибору міста: фіксація обраного міста/відділення/вулиці в сесії без повторного запиту при подальших текстових змінах оцінки чи опису.
   - [x] Розширення `heal_parsed_recipient_info` регулярними виразами для автоматичного розпізнавання оцінки (`declared_value`), опису вантажу (`cargo_description`), наложки (`cod_amount`) та платника.

8. [x] **Відстеження накладеного платежу, місячні ліміти та статистика (v0.22.0)**:
   - [x] Моделі `CODItemInfo` та `CODMonthlyStats` у `src/nova_poshta/models.py`.
   - [x] Отримання місячної статистики через `InternetDocument/getDocumentList` у `src/nova_poshta/client.py`.
   - [x] Збереження лімітів суми та кількості посилок у `src/storage.py` з налаштуваннями за замовчуванням (30 000 грн / 10 посилок).
   - [x] Кнопка `💰 Накладений платіж` та дашборд статистики з прогрес-барами та розбивкою статусів (виплачено, в дорозі, відмови).
   - [x] Автоматичне обнулення на початку кожного місяця та попередження при створенні ТТН з перевищенням ліміту.

9. [x] **Безпечна межа (< 30 000 / макс. 29 999 грн), live-опитування та список ТТН (v0.22.1)**:
   - [x] Встановлення безпечного порогу `sum_limit - 1` (29 999 грн) у дашборді та попередженнях.
   - [x] Пряме live-опитування API Нової Пошти без кешування при кожному створенні/редагуванні ТТН з наложкою.
   - [x] Функція `_extract_float_amount` для витягу сум післяплати з усіх форматів та полів API.
   - [x] Кнопка `[ 📜 Накладні та суми наложки (ТТН) ]` прямо під постом дашборду для детального перегляду.

10. [x] **Суворе розпізнавання грошової післяплати без врахування тарифів доставки (v0.22.2)**:
    - [x] Виключення тарифів доставки (`Cost`, `CostOnSite`, `BackwardDeliveryCost`) з підрахунку післяплати.
    - [x] Суворий аналіз `CargoType in ("Money", ...)` у `BackwardDeliveryData` та поля `AfterpaymentOnGoodsCost`.

11. [x] **Універсальне відстеження будь-яких накладних (ТТН) через API Нової Пошти (v0.23.0)**:
    - [x] Pydantic-модель даних `TrackingDocumentDetails` у `src/nova_poshta/models.py` з усіма полями API.
    - [x] Метод `track_document` у `src/nova_poshta/client.py` через `TrackingDocument/getStatusDocuments`.
    - [x] Інформативна Telegram-картка `format_tracking_card` з усіма деталями (статус, повний маршрут, хронологія, параметри вантажу, фінанси, післяплата, легке повернення).
    - [x] Автоматичне розпізнавання 14-значних та 11-значних номерів ТТН з тексту та інтенту відстеження.
    - [x] Кнопка `🔍 Відстежити ТТН` у головному меню та команди `/track`, `/tracking`.
    - [x] Інтерактивні кнопки: `📱 Показати штрихкод` (Code128 для сканування на касі), `🔄 Оновити статус`, пряме посилання на веб-сайт НП.

12. [x] **Виправлення сумісності з Python 3.12 (v0.23.1)**:
    - [x] Імпорт `Dict` з модуля `typing` у `src/nova_poshta/models.py` для усунення `NameError: name 'Dict' is not defined` при старті сервісу на Ubuntu 24.04 (Python 3.12).
    - [x] Регресійне тестування type hints через `typing.get_type_hints` у `tests/test_tracking.py`.

13. [x] **Виправлення генерації штрихкоду та вилучення накладних з реєстру (ScanSheet) через AI (v0.23.2)**:
    - [x] Захист `generate_code128_barcode` від порожнього рядка (`ValueError`) для усунення `IndexError: list index out of range`.
    - [x] Валідація результатів `create_scan_sheet` у клієнті Нової Пошти (перевірка `Errors`, непорожнього `Number`).
    - [x] Реалізація методу `remove_documents_from_scan_sheet` (`ScanSheet/removeDocuments`) у `NovaPoshtaClient`.
    - [x] Додавання дій `remove_waybill` та `delete_register` у схеми `AIRegisterFilterResult`, `ParsedRecipientInfo` та промпт `REGISTER_FILTER_SYSTEM_PROMPT`.
    - [x] Збереження контексту останнього створеного/переглянутого реєстру (`USER_LAST_SCANSHEET_CONTEXT`) для підтримки природномовного вилучення накладних за порядковим номером (№1, №2...), номером ТТН або прізвищем.
    - [x] Оновлення збереженого реєстру в `storage.py` та генерація оновленого реєстру зі штрихкодом.

14. [x] **Виправлення вибору реєстру, очищення пошкоджених записів та передача Ref реєстру (v0.23.3)**:
    - [x] Автоматична фільтрація та метод `cleanup_invalid_scansheets` у `storage.py` для очищення записів з порожнім `number` або `ref`.
    - [x] Метод `get_scan_sheet_documents` у `NovaPoshtaClient` для отримання списку документів та точних GUID `Ref`.
    - [x] Передача `scan_sheet_ref` у `ScanSheet/removeDocuments` API Нової Пошти.
    - [x] Надійне відновлення активного реєстру з бази при порожньому контексті в пам'яті (усунення вибору фантомних записів) та коректне зменшення списку накладних з 3 до 2.

15. [x] **Інтелектуальний комбінований пошук вулиць через searchSettlementStreets та getStreet (v0.23.4)**:
    - [x] Додавання поля `settlement_ref` у `CityInfo` (`src/nova_poshta/models.py`).
    - [x] Кешування `_city_settlement_cache` та метод `get_settlement_ref` у `NovaPoshtaClient` (`src/nova_poshta/client.py`).
    - [x] Автоматичне зв'язування `DeliveryCity -> SettlementRef` через `searchSettlements` у `search_city`.
    - [x] Багаторівневий пошук у `search_street`: прямий `getStreet`, повнотекстовий `searchSettlementStreets` (як в офіційному мобільному додатку Нової Пошти) та фолбек-розширення популярними званнями/титулами (Генерала, Академіка, Гетьмана, Степана, Богдана тощо).
    - [x] Покращене ранжування кандидатів з пріоритетом точного збігу та входження підрядка.
    - [x] Передача `settlement_ref` та `city_name` з `matched_city` до `search_street` у `src/bot/handlers.py`.
    - [x] Тестування пошуку скорочених та народних назв вулиць (`"Тарнавського"` -> `"вул. Генерала Тарнавського"`, `"Хмельницького"` -> `"вул. Богдана Хмельницького"`).

16. [x] **Виправлення розбіжності кількості накладних у створеному реєстрі (ScanSheet) та фільтрація вже зареєстрованих чернеток (v0.23.5)**:
    - [x] Розширення `ScanSheetInfo` полями `success_documents: List[str]` та `error_documents: List[Dict[str, Any]]` з конфігурацією `populate_by_name`.
    - [x] Додавання поля `scan_sheet_number: Optional[str] = None` у `WaybillItemInfo` та `SavedDraft`.
    - [x] Метод `update_drafts_scansheet` у `UserSettingsManager` для зв'язування чернеток із реєстром.
    - [x] Глибокий аналіз та зчитування `Data.Success`, `Data.Errors`, `Data.Warnings` у `create_scan_sheet`, що усуває хибний підрахунок `len(document_refs)` при часткових помилках додавання накладних.
    - [x] Зчитування поля `ScanSheetNumber` з відповідей `InternetDocument/getDocumentList` для виявлення ТТН, що вже перебувають в іншому реєстрі.
    - [x] Оновлення `REGISTER_FILTER_SYSTEM_PROMPT` для ігнорування чернеток, що вже мають реєстр, при формуванні нових загальних реєстрів.
    - [x] Включення у створений реєстр та контекст Telegram-бота виключно фактично доданих ТТН із виведенням інформативного блоку `⚠️ Не додано до цього реєстру:` для відхилених накладних.
    - [x] Синхронізація лічильника накладних у `cmd_registers` з актуальними даними Nova Poshta API.
    - [x] 81 юніт-тест виконано паралельно (`pytest -n auto`).

17. [x] **Виправлення UnboundLocalError при зміні виплати наложки або оцінки (v0.23.6)**:
    - [x] Перенесення ініціалізації `eff_settings` та `user_np_client` на початок `process_waybill_callback` у `src/bot/handlers.py`.
    - [x] Усунення падіння `UnboundLocalError: cannot access local variable 'user_np_client'` при натисканні inline-кнопок `toggle_cod_type` (перемикання на виплату на картку/готівкою), `cycle_cod`, `cycle_value`.
    - [x] Додавання регресійного юніт-тесту `test_waybill_action_toggle_cod_type_and_cycles_no_unbound_local_error` у `tests/test_active_session_and_updates.py`.
    - [x] 82 юніт-тести виконано паралельно (`pytest -n auto`).

18. [x] **Автоматичний контроль ліміту післяплати при додаванні накладної (v0.23.7)**:
    - [x] Модульна функція `evaluate_cod_limits` у `src/bot/handlers.py` для розрахунку використаних коштів і посилок через live API `get_monthly_cod_stats`.
    - [x] Захист від подвійного підрахунку старої суми наложки при редагуванні чинних чернеток (`editing_ref`).
    - [x] Відображення в картці перевірки ТТН детального блоку контролю: використано наразі, сума цієї ТТН, новий підсумок, точний залишок або сума перевищення межі.
    - [x] Клавіатура `get_limit_exceeded_confirmation_keyboard` у `src/bot/keyboards.py` з діями `force_confirm`, `cycle_cod`, `back_to_card`, `cancel`.
    - [x] Захисне перехоплення дії `action == "confirm"` у разі перетину ліміту та виведення екрана підтвердження з попередженням про фінмоніторинг NovaPay.
    - [x] Обробка дії `action == "force_confirm"` для свідомого створення ТТН попри перевищення ліміту.
    - [x] Додавання оновленого балансу післяплати за місяць у фінальну картку створеної ТТН.
    - [x] Виправлення друкарської помилки формування заголовка успішного створення накладної.
    - [x] 90 юніт-тестів виконано паралельно (`pytest -n auto`).

19. [x] **Мультиакаунти (користувачі/відправники), кнопка «Користувачі» та інтелектуальний розподіл лімітів післяплати (v0.24.0)**:
    - [x] Модель `SenderProfile` у `src/storage.py` та розширення `UserCustomSettings` списком `profiles` і активним профілем `active_profile_id`.
    - [x] Автоматична міграція наявних налаштувань у перший профіль користувача зі збереженням 100% сумісності.
    - [x] Методи `get_sender_profiles`, `set_active_profile`, `add_sender_profile`, `delete_sender_profile` у `UserSettingsManager`.
    - [x] Постійна кнопка `👥 Користувачі` у головному меню `get_main_reply_keyboard()` та команди `/users`, `/profiles`, `/add_user`.
    - [x] Інтерактивний дашборд користувачів із живими балансами наложки за місяць, залишками безпечного ліміту та перемиканням активного профілю в один клік.
    - [x] Додавання нових профілів відправників через API-ключ Нової Пошти з автозаповненням ПІБ, телефону та контрагента.
    - [x] Функція `evaluate_all_profiles_cod_limits` для пошуку користувача з найбільшим доступним залишком ліміту.
    - [x] Інтелектуальна пропозиція переключитися на іншого користувача при наближенні/перетині межі 30 000 грн та при переході до післяплати.
    - [x] Швидке перемикання відправника безпосередньо у картці підтвердження ТТН та на екрані блокування перевищення ліміту.
    - [x] Юніт-тести для мультипрофілів, перемикання та розподілу лімітів післяплати у паралельному режимі (`pytest -n auto`).

20. [x] **Швидкі дії для накладної: вибір між «Відстежити» та «Згенерувати штрих-код» (v0.24.1)**:
    - [x] Розширення схеми `TrackActionCallback` діями `track`, `refresh`, `barcode`.
    - [x] Меню вибору дій `get_waybill_action_keyboard(doc_number)` з виділеними кнопками `[ 🔍 Відстежити ]` та `[ 📱 Згенерувати штрих-код ]` при надсиланні номера накладної без тексту.
    - [x] Оновлення кнопки в картці відстеження на `📱 Згенерувати штрих-код` та додавання кнопки швидкого переходу `[ 🔍 Відстежити ТТН ]` під фотографією штрих-коду.
    - [x] Функція `send_waybill_barcode` для генерації та відправлення зображення Code128 у чат Telegram.
    - [x] Прямі команди `/barcode [ТТН]`, `/штрихкод`, `/code128` та діалоговий стан очікування `USER_BARCODE_WAITING`.
    - [x] Визначення природномовного наміру отримання штрих-коду `is_barcode_intent(text)`.
    - [x] Покриття юніт-тестами в `tests/test_tracking.py` та паралельне проходження всіх 108 тестів (`pytest -n auto`).

21. [x] **Інтелектуальне визначення населеного пункту та відділення за адресою через AI (v0.24.2)**:
    - [x] Додавання Pydantic-моделі `AICandidateDisambiguationResult` у `src/ai/schemas.py`.
    - [x] Системний промпт `DISAMBIGUATION_SYSTEM_PROMPT` у `src/ai/extractor.py` для порівняння тексту повідомлення зі знайденими кандидатами населених пунктів та відділень/поштоматів.
    - [x] Метод `disambiguate_candidates` у `AIExtractor` для асинхронного AI-аналізу кандидатів через структурований JSON.
    - [x] Стійкий евристичний fallback `heuristic_disambiguate_candidates` для точного токенного співставлення вулиці, номера будинку та району при відсутності зв'язку з AI.
    - [x] Збереження та прокидування вихідного тексту `raw_text` користувача через усі етапи сесії та обробника `_continue_processing_recipient_info`.
    - [x] Автоматичне застосування обраного варіанта без показу інтерактивної клавіатури, якщо адреса в повідомленні однозначно збігається з одним із кандидатів.
    - [x] Збереження показу клавіатури `get_city_selection_keyboard` при відсутності адреси або низькій впевненості.
    - [x] 113 юніт-тестів пройдено успішно в паралельному режимі (`pytest -n auto`).

22. [x] **Збереження першочергового повідомлення та мультидіалогова дисамбігуація населених пунктів за адресою (v0.24.3)**:
    - [x] Збереження першочергового повідомлення (`initial_raw_text`) та повної історії реквізитів (`all_raw_texts`) у сесії `PENDING_SESSIONS` та словнику `USER_INITIAL_MESSAGE_TEXT`.
    - [x] Автоматичне збереження та відновлення тексту реквізитів при мультидіалогових запитах (коли окремими повідомленнями надсилаються спочатку реквізити отримувача з адресою, а потім оцінка/опис або інші уточнення).
    - [x] Передача повного комбінованого тексту всіх повідомлень сесії до AI-дизамбігуатора `disambiguate_candidates` та евристики.
    - [x] Розширення евристики `heuristic_disambiguate_candidates`: точний парсинг вулиць після двокрапки та перед комою, відсікання префіксів (`вул.`, `просп.`, `пров.`, `бульв.`), зіставлення номерів будинків з літерами (наприклад, `13а`, `237`).
    - [x] Очищення `USER_INITIAL_MESSAGE_TEXT` при скиданні та завершенні активної сесії (`clear_user_active_session` та `_cleanup_expired_sessions`).
    - [x] Додавання мультидіалогового юніт-тесту `test_auto_disambiguate_settlement_multi_turn_initial_message` у `tests/test_active_session_and_updates.py`.
    - [x] Успішне паралельне проходження всіх 114 юніт-тестів (`pytest -n auto`).

23. [x] **Розширене діагностичне логування дисамбігуації кандидатів та моніторинг виконання (v0.24.4)**:
    - [x] Додавання детального логування `Attempting candidate disambiguation` із вмістом запиту та кількістю кандидатів.
    - [x] Додавання логування підсумку `Candidate disambiguation result: chosen_idx` для миттєвої діагностики через `journalctl`.
    - [x] 114 юніт-тестів пройдено успішно в паралельному режимі (`pytest -n auto`).

24. [x] **Усунення стану перегонів у дебаунсері та захист активних задач від скасування (v0.24.5)**:
    - [x] Розподіл дебаунсингу повідомлень на фазу накопичення/очікування (1.0с) та фазу послідовного виконання під блокуванням `asyncio.Lock` за `user_id`.
    - [x] Автоматичне вилучення задачі з `USER_DEBOUNCE_TASKS` одразу після завершення 1.0с очікування для унеможливлення переривання активної обробки AI та API наступними повідомленнями.
    - [x] Забезпечення черговості обробки доповнень (наприклад, надсилання оцінки й опису під час активного формування ТТН) без втрати контексту активної сесії та без хибного спрацьовування повідомлень про відсутність реквізитів (`ПІБ: N/A, Телефон: Не вказано`).
    - [x] Регресійний юніт-тест `test_followup_message_does_not_cancel_active_processing_task` у `tests/test_active_session_and_updates.py`.
    - [x] 115 юніт-тестів виконано успішно в паралельному режимі (`pytest -n auto`).

25. [x] **Нормалізація українського апострофа в пошуку Нової Пошти, AI-екстракторі та валідаторах (v0.24.6)**:
    - [x] Створення модуля `src/utils/text_cleaner.py` з нормалізацією всіх Unicode-варіантів апострофів (`\u02bc`, `\u2019`, `\u2018`, `\u0060`, `\u00b4`, `\u02bb`, `\u02b9`, `\u2032`, `\u2033`, `\u201b`) до стандартного ASCII `'` (`\u0027`), генерацією фонетичних варіацій (після губних та `р` перед `яюєї` для слів без апострофа) та функцією `is_city_matched`.
    - [x] Інтеграція нормалізації та варіативного пошуку в `NovaPoshtaClient` (`search_city`, `search_street`, `get_settlement_ref`, `create_recipient_counterparty`).
    - [x] Додавання валідатора нормалізації апострофів у Pydantic-модель `ParsedRecipientInfo`.
    - [x] Нормалізація вхідного тексту та оновлення регулярних виразів у `AIExtractor` (`heal_parsed_recipient_info` та `parse_text`) з усуненням міжрядкового захоплення імен.
    - [x] Оновлення обробників `src/bot/handlers.py` для використання нормалізованого зіставлення міст.
    - [x] Розробка юніт-тестів `tests/test_apostrophes.py` та верифікація через паралельний запуск тестів (`pytest -n auto` -> 124 пройдено).

26. [x] **Виправлення TelegramBadRequest при додаванні користувача, постійна кнопка Меню в Desktop/Mobile, редизайн кнопок дашборду та цифрова Картка клієнта (v0.24.7)**:
    - [x] Виправлення помилки `TelegramBadRequest` у методі `_handle_add_profile_with_key`: коректне видалення повідомлення очікування `status_msg` та відправка фінального повідомлення через `message.answer` із передачею `ReplyKeyboardMarkup`.
    - [x] Реєстрація системних команд бота через `bot.set_my_commands` та активація постійної кнопки меню `bot.set_chat_menu_button` у `src/bot/main.py` для гарантованого відображення кнопки «Меню» в десктопному Telegram Desktop та мобільних клієнтах.
    - [x] Очищення та оптимізація кнопок вибору відправника в `get_users_management_keyboard`: прибрано зайвий префікс «Обрати:» та позначку «(Активний)», залишок ліміту винесено на окремий другий рядок кнопки (`\n`), що запобігає обрізанню тексту на смартфонах.
    - [x] Інтеграція методу `get_loyalty_info` у `src/nova_poshta/client.py` за допомогою офіційного API Нової Пошти `LoyaltyUser/getLoyaltyInfoByApiKey` (витяг `LoyaltyCard`, `UserLogin`, `FullName`, `Phone`, `Discount`).
    - [x] Розробка функції генерації цифрової картки клієнта `generate_client_card_image` у `src/utils/barcode_gen.py` з фірмовим брендингом Нової Пошти, ПІБ, телефоном, номером картки/CID та високоякісним штрих-кодом Code128 (300 DPI) для безпомилкового зчитування сканерами у відділеннях.
    - [x] Додавання команди `/client_card`, інлайн-кнопки в налаштуваннях (`⚙️ Налаштування`), кнопки у головній клавіатурі (`💳 Картка клієнта`) та інтерактивного перемикання штрихкоду між номером телефону та номером картки лояльності (CID).
    - [x] Написання комплексу юніт-тестів у `tests/test_client_card_and_ui.py` та успішне паралельне проходження всіх 132 тестів проекту (`python -m pytest -n auto`).

27. [x] **Збереження та плавне відновлення сесії створення ТТН при додаванні або зміні банківської картки (v0.24.8)**:
    - [x] Усунення деструктивного виклику `clear_user_active_session` з обробника команди `/set_card` та допоміжних команд конфігурації (`/client_card`, `/set_cod_limit`, `/set_ai_model`).
    - [x] Реалізація централізованого рендерера `_build_waybill_preview_message` у `src/bot/handlers.py` для генерації актуального тексту перевірки ТТН та інлайн-клавіатури дій.
    - [x] Створення функції `_save_user_card_and_resume_session`: автоматичне оновлення сесії (`cod_payment_type="card"`), редагування попереднього повідомлення чернетки та відправка свіжої картки ТТН з інтерактивними кнопками підтвердження для безперервного процесу оформлення.
    - [x] Підтримка прямого відправлення 16-значного номера картки в чат або через стан очікування `USER_CARD_WAITING` без необхідності ручного повторного введення даних ТТН.
    - [x] Автоматичний пошук та підключення зареєстрованих карток контрагента з кабінету Нової Пошти через `get_payment_cards` при перемиканні на виплату на картку.
    - [x] Додавання підтримки параметрів `cod_payment_type` та `payment_card` (`RedeliveryPaymentCard`) у метод `create_waybill` клієнта API Нової Пошти.
    - [x] Написання тестового набору `tests/test_card_session_resume.py` та успішне паралельне виконання 136 тестів (`python -m pytest -n auto`).

28. [x] **Автоматичне підтягування адреси відправлення з сайту/API, налаштування в додатку з вищим пріоритетом та захист від збоїв генерації ТТН (v0.24.9)**:
    - [x] Реалізація методу `fetch_sender_address_from_api(counterparty_ref)` у `NovaPoshtaClient` з перевіркою адрес контрагента через `Counterparty/getCounterpartyAddresses` (Sender, Recipient) та резервним аналізом останніх оформлених документів через `InternetDocument/getDocumentList` (`CitySender`, `SenderAddress`, `CitySenderDescription`, `SenderAddressDescription`).
    - [x] Інтеграція підтягування адреси відправлення у метод `fetch_sender_profile` при додаванні нового відправника (`/add_user`) або повторній синхронізації.
    - [x] Додавання полів `api_sender_city_ref`, `api_sender_city_name`, `api_sender_address_ref`, `api_sender_warehouse_name` у моделі `SenderProfile` та `UserCustomSettings` у `src/storage.py`.
    - [x] Реалізація строгої багаторівневої ієрархії пріоритетів у методі `get_effective_settings`:
      1. Адреса, явно налаштована користувачем у додатку (`/set_city` та `/set_warehouse`) на цільовому профілі — **найвищий пріоритет**.
      2. Адреса відправлення, отримана з сайту/API для цього профілю.
      3. Адреса активного профілю (в додатку, потім API).
      4. Інші профілі користувача (в додатку, потім API).
      5. Налаштування користувача верхнього рівня.
      6. Глобальні налаштування системи.
    - [x] Автоматична міграція та спадкування відсутніх адрес у `_ensure_profile_migration` від налаштованих профілів-донорів або збережених API-адрес.
    - [x] Впровадження pre-flight валідації в обробник `confirm` / `force_confirm` перед зверненням до API Нової Пошти: перевірка наявності `sender_city_ref` та `sender_address_ref`. При відсутності — блокування критичної помилки `CitySender not selected, SenderAddress not selected`, збереження активної чернетки ТТН, виведення інструкцій (`/set_city`, `/set_warehouse`) та кнопок `[ 🔄 Спробувати знову ]` і `[ ❌ Скасувати ]`.
    - [x] Усунення деструктивного виклику `clear_user_active_session` з команд `/set_city` та `/set_warehouse`.
    - [x] Автоматичне відновлення та повторний показ оновленої картки перевірки ТТН після успішного встановлення відділення через `/set_warehouse` за наявності незавершеної чернетки.
    - [x] Додавання команди `/sync_address` (`/sync_sender_address`) та інлайн-кнопки `[ 🔄 Підтягнути адресу з сайту (API) ]` у картку керування відправником дашборду `👥 Користувачі`.
    - [x] Візуальне відображення джерела відділення відправки у дашборді профілю: `(в додатку)` або `(з сайту/API)`.
    - [x] Комплексний тестовий набір `tests/test_sender_address_api_and_priority.py` (7 нових тестів) та успішний паралельний запуск усіх 143 тестів проекту (`python -m pytest -n auto`).

29. [x] **Інтеграція створених у відділенні накладних (5900...) через `getOutgoingDocumentsByPhone` та `getIncomingDocumentsByPhone`, точний підрахунок післяплати та оптимізація кешування (v0.24.10)**:
    - [x] Інтеграція нативних методів API Нової Пошти `InternetDocument/getOutgoingDocumentsByPhone` та `InternetDocument/getIncomingDocumentsByPhone` для виявлення посилок, створених оператором у відділенні (`5900...`), поряд з інтернет-накладними (`2045...`).
    - [x] Реалізація допоміжної функції `_normalize_phone_doc` у `src/nova_poshta/client.py` для конвертації форматів відповідей телефонних запитів у загальний внутрішній словник документа.
    - [x] Оновлення `_fetch_raw_waybills_with_cache`: злиття результатів `getDocumentList` та `getOutgoingDocumentsByPhone` з глобальним кешем за API-ключем (300с).
    - [x] Реалізація `_fetch_raw_incoming_waybills_with_cache` та інтеграція `getIncomingDocumentsByPhone` у `get_incoming_waybills` для повноцінного відображення вхідних посилок у дорозі.
    - [x] Інтеграція `getOutgoingDocumentsByPhone` у розрахунок місячної статистики післяплати `get_monthly_cod_stats`: автоматичне виявлення створених у відділенні відправлень з післяплатою (включно з післяплатою на картку 14 800 грн), їх категоризація за статусами та списання з щомісячного ліміту 29 999 грн.
    - [x] Виправлення наслідування API-ключа в `get_effective_settings` (`src/storage.py`): профілі без власного ключа безперешкодно наслідують `user_custom.nova_poshta_api_key`.
    - [x] Написання юніт-тестів `tests/test_nova_poshta.py` для нормалізації даних та обробки накладних з відділення.
    - [x] Успішне паралельне виконання всіх 146 тестів проекту (`python -m pytest -n auto`).