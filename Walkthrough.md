# Walkthrough (Журнал змін)

## [v0.19.3] - 2026-08-09
- **Покращення видалення ТТН та обробка накладних, включених до реєстру**:
  - У `delete_waybill` (`src/nova_poshta/client.py`) параметр `DocumentRefs` тепер гарантовано передається як масив `[document_ref]` згідно зі специфікацією API Нової Пошти.
  - У `process_draft_callback` (`src/bot/handlers.py`) додано зрозуміле та інформативне попередження користувачеві при спробі видалити ТТН, яка наразі включена до активного реєстру:
    `⚠️ Не вдалося видалити ТТН. Якщо ця накладна включена до реєстру (ScanSheet), спочатку розформуйте реєстр нижче.`
- **Тестування**:
  - Додано юніт-тест `test_delete_waybill_method` у `tests/test_nova_poshta.py`.
  - Усі 31 модульний тест виконано успішно (`31 passed`).

## [v0.19.2] - 2026-08-09
- **Виправлення видалення/розформування реєстру з фотоповідомлення (підтримка `edit_caption`)**:
  - У `process_register_callback` (`src/bot/handlers.py`) виправлено обробку кнопки розформування реєстру `action == "delete"`. Оскільки створений реєстр надсилається як фото зі штрихкодом (має `caption`, а не `text`), прямий виклик `edit_text` викликав помилку `TelegramBadRequest: Bad Request: there is no text in the message to edit`.
  - Додано коректну перевірку: якщо повідомлення містить фото/підпис (`photo` або `caption`), викликається `callback.message.edit_caption(...)`, інакше `callback.message.edit_text(...)` з fallback на `message.answer`.
- **Тестування**:
  - Додано юніт-тест `test_process_register_callback_deletes_caption_or_text` у `tests/test_scan_sheet.py`.
  - Усі 30 модульних тестів виконано успішно (`30 passed`).

## [v0.19.1] - 2026-08-09
- **Виправлення виклику створення реєстру в API Нової Пошти (`ScanSheet/insertDocuments`)**:
  - У `create_scan_sheet` (`src/nova_poshta/client.py`) виправлено назву методу API з неіснуючого `ScanSheet/save` на офіційний метод `ScanSheet/insertDocuments`.
  - У `delete_scan_sheet` (`src/nova_poshta/client.py`) оновлено параметр з `ScanSheetRef` на `ScanSheetRefs: [scan_sheet_ref]`.
  - Усунуто помилку API Нової Пошти `Method ScanSheetGeneral_save not found, Validator for method ScanSheetGeneral_save not found`.
- **Тестування**:
  - Додано юніт-тест `test_create_and_delete_scan_sheet_methods` у `tests/test_nova_poshta.py`.
  - Усі 29 модульних тестів виконано успішно (`29 passed`).

## [v0.19.0] - 2026-08-09
- **Інтелектуальне формування реєстрів (ScanSheet) через AI зі структурованим JSON**:
  - **AI Schemas (`src/ai/schemas.py`)**: додано Pydantic модель `AIRegisterFilterResult` для структурованої відповіді ШІ (`action`, `selected_doc_numbers`, `summary`, `explanation`).
  - **AI Extractor (`src/ai/extractor.py`)**: додано метод `filter_drafts_for_register` з системним промптом `REGISTER_FILTER_SYSTEM_PROMPT`. ШІ отримує контекст поточного часу (`current_timestamp`, `day_of_week`) та повний масив невідправлених чернеток користувача у форматі JSON. ШІ підтримує довільні запити:
    - Вибір конкретних ТТН (наприклад: *"Створи реєстр з накладної 20451506611097"*).
    - Вибір усіх чернеток (наприклад: *"Створи реєстр з усіх моїх чернеток"*).
    - Часова фільтрація (наприклад: *"створені вчора"*, *"вчора до 12:00"*, *"за сьогодні"*).
    - Фільтрація за містом, отримувачем, описом вантажу чи накладеним платежем.
  - **Єдина синхронізація чернеток (`fetch_user_active_drafts`) у `src/bot/handlers.py`**:
    - Пакетне отримання чернеток з кабінету Нової Пошти (`get_internet_document_list`) та локального сховища.
    - Перевірка статусів відправлення через `getStatusDocuments` з автоматичним відсіюванням та очищенням уже відправлених посилок.
    - `cmd_drafts` та створення реєстрів використовують єдине перевірене джерело чернеток.
  - **Детальний опис накладних у створеному реєстрі**:
    - Після успішного створення реєстру (`create_scan_sheet`) бот повертає високоякісний штрих-код Code128 у форматі PNG, номер реєстру, дату, кількість та структурований список кожної доданої ТТН (номер, ПІБ отримувача, місто/відділення, опис посилки, оцінка та накладений платіж).
- **Тестування**:
  - Додано тести `test_ai_register_filter_result_model`, `test_filter_drafts_for_register_mocked`, `test_filter_drafts_for_register_fallback` у `tests/test_ai_extractor.py`.
  - Додано тест `test_fetch_user_active_drafts_combines_and_filters` у `tests/test_scan_sheet.py`.
  - Усі 28 модульних тестів виконано успішно (`28 passed`).
- **Точне відображення Оцінки та Накладеного платежу у картці чернетки**:
  - У `get_internet_document_list` (`src/nova_poshta/client.py`) виправлено парсинг:
    - `Cost` / `DeclaredCost` (реальна оціночна вартість, наприклад 15 000 грн) більше не перетирається доставковим тарифом або дефолтним 500 грн.
    - Витягується сума накладеного платежу з `BackwardDeliveryData`, `AfterpaymentOnGoodsCost`, `RedeliveryString`, `BackwardDeliveryMoney`.
    - Витягується спосіб виплати наложки (на банківську картку чи готівкою).
  - У `cmd_drafts` (`src/bot/handlers.py`) додано відображення рядка:
    `💵 Накладений платіж: 15000 грн (Картка)` та реальної оціночної вартості.
- **Тестування**: усі 24 модульні тести виконано успішно (`24 passed`).

## [v0.18.3] - 2026-08-08
- **Фільтрація лише невідправлених чернеток ТТН**:
  - У `cmd_drafts` (`src/bot/handlers.py`) додано масову перевірку статусів усіх документів через `get_documents_status`.
  - Усі ТТН, які вже фізично відправлені, прийняті у відділенні, прямують або доставлені (`is_shipped == True`), автоматично виключаються зі списку чернеток.
  - У списку `📝 Мої чернетки (ТТН)` відображаються виключно активні невідправлені електронні накладні.
- **Тестування**: усі 24 модульні тести виконано успішно (`24 passed`).

## [v0.18.2] - 2026-08-08
- **Виправлення назви метода API Нової Пошти (`getDocumentList`)**:
  - Метод `getInternetDocumentList` не існує в API Нової Пошти. Виправлено на коректну назву `getDocumentList` у `src/nova_poshta/client.py`.
- **Тестування**: усі 24 модульні тести виконано успішно (`24 passed`).

## [v0.18.1] - 2026-08-08
- **Виправлення імпорту `Optional` у `src/bot/keyboards.py`**:
  - Усунуто помилку `NameError: name 'Optional' is not defined` при імпорті `keyboards.py` в Linux/Systemd середовищах.
  - Явно додано `from typing import Optional, List, Dict, Any` у `src/bot/keyboards.py`.
- **Тестування**: перевірено імпорт `python -c "import src.bot.main"` та виконання pytest (`24 passed`).

## [v0.18.0] - 2026-08-08
- **Пряме підтягування невідправлених чернеток з сервера Нової Пошти**:
  - У `NovaPoshtaClient` (`src/nova_poshta/client.py`) додано метод `get_internet_document_list()`, що звертається до ендпоінта `InternetDocument/getInternetDocumentList`.
  - У `cmd_drafts` (`/drafts` / `📝 Мої чернетки (ТТН)`) реалізовано об'єднання створених через бота чернеток та створених/редагованих безпосередньо на сайті Нової Пошти.
  - Оновлено вирахування статусу `is_shipped` у `get_documents_status`: чернетки зі статус-кодами 1, 2, 3 (електронні ТТН до передачі у відділення) більше не видаляються з пам'яті.
- **Зручний вибір способу виплати накладеного платежу (Готівка / Картка)**:
  - У клавіатуру перевірки ТТН (`src/bot/keyboards.py`) додано інтерактивну кнопку `🔄 💳 Виплата: На картку (4149...1234)` / `🔄 💵 Виплата: Готівкою у відділенні`.
  - Реалізовано перевірку наявності картки: якщо користувач обирає виплату на картку, а картку ще не збережено, бот виводить дружній покажчик для збереження картки (`/set_card`).
  - Додано автоматичне виявлення 16-значного номера картки у текстових повідомленнях чату з авто-збереженням у профілі.
- **Тестування**: усі 24 модульні тести виконано успішно (`24 passed`).

## [v0.17.1] - 2026-08-08
- **Автоматична вимога `declared_value >= cod_amount` (Оцінка товару >= Наложки)**:
  - У `src/bot/handlers.py` та `src/nova_poshta/client.py` додано автоматичну синхронізацію оціночної вартості з сумою накладеного платежу:
    - При парсингу ШІ або первинній обробці: `declared_value = max(declared_value, cod_amount)`.
    - При зміні наложки кнопкою `cycle_cod`: якщо наложка перевищує поточну оцінку, оцінка автоматично підтягується до суми наложки.
    - При зміні оцінки кнопкою `cycle_value`: оцінка не може опуститися нижче за суму наявного накладеного платежу.
- **Тестування**: усі 24 модульні тести виконано успішно (`24 passed`).

## [v0.17.0] - 2026-08-08
- **Точний штрихкод реєстру (ScanSheet)**:
  - У `generate_code128_barcode` (`src/utils/barcode_gen.py`) прибрано видалення дефісів. Текст реєстру кодується у форматі Code128 з точним збереженням дефісів (наприклад, `105-79184007`). Згенерований у Telegram штрихкод відтепер 100% сканується сканерами Нової Пошти.
- **Підтримка Накладеного платежу (COD / Грошовий переказ)**:
  - **AI Extractor (`src/ai/extractor.py`, `src/ai/schemas.py`)**: додано розпізнавання суми накладеного платежу (`cod_amount`) та типу виплати (`cod_payment_type`: готівка/картка) з текстів користувача (наприклад, *"наложка 1500 грн"*).
  - **Nova Poshta API Client (`src/nova_poshta/client.py`)**: додано підтримку масиву зворотної доставки `BackwardDeliveryData` при використанні `cod_amount > 0` та метод `get_payment_cards` для отримання банківських карт відправника.
  - **Інтерфейс бота (`src/bot/handlers.py`, `src/bot/keyboards.py`)**: додано інноваційну кнопку `🔄 💰 Наложка: 1500 грн` у картку перевірки ТТН (циклічний вибір суми: 0, 500, 1000, 1500, 2000, 3000 грн) та команду `/set_card` для збереження банківської картки за замовчуванням.
- **Тестування**: оновлено `tests/test_scan_sheet.py` для перевірки збереження дефісів у Code128. Усі 24 тести виконано успішно (`24 passed`).

## [v0.16.1] - 2026-08-08
- **Виправлення помилки `AttributeError: 'Settings' object has no attribute 'sender_name'`**:
  - Додано поле `sender_name: str = Field("", alias="SENDER_NAME")` у модель конфігурацій `Settings` (`src/config.py`).
  - Оновлено `UserSettingsManager.get_effective_settings` (`src/storage.py`): додано коректне перенесення `sender_name` у об'єкт `Settings` користувача.
  - У `src/bot/handlers.py` додано санітизацію тексту помилок перед надсиланням у Telegram (`clean_err = str(e).replace("*", "")...`), що запобігає падінню Telegram-парсерів (`TelegramBadRequest: can't parse entities`).
- **Тестування**: усі 24 модульні тести виконано успішно (`24 passed`).

## [v0.16.0] - 2026-08-08
- **Повна ізоляція AI-провайдера для кожного користувача Telegram**:
  - У `UserSettingsManager` (`src/storage.py`) скасовано фолбек на глобальні AI ключі з `.env` (`AI_API_KEY`, `AI_BASE_URL`).
  - Оновлено `is_user_configured`: користувач вважається налаштованим тільки за наявності **двох персональних ключів** — ключа Нової Пошти та AI API-ключа.
  - Оновлено захисник-онбординг `ensure_user_configured`: інформує про відсутні ключі (НП або AI) з прямими підказками команд.
  - Додано нові команди керування AI: `/set_ai_key` (встановлення ключа), `/set_ai_url` (власна адреса ендпоінта AI), `/set_ai_model` (назва AI моделі).
  - Оновлено вивід у `/settings` / `/profile` / `⚙️ Налаштування` з повним блоком даних AI-провайдера.
- **Тестування**: оновлено `tests/test_storage.py` для перевірки строгої ізоляції AI конфігів. Усі 24 тести виконано успішно (`24 passed`).

## [v0.15.1] - 2026-08-08
- **Розпізнавання реального ПІБ відправника замість "Приватна особа" та суворий фільтр за номером телефону**:
  - У `NovaPoshtaClient.fetch_sender_profile` додано логіку формування ПІБ з контактної особи (`LastName + FirstName + MiddleName`). Якщо опис контрагента містить `"Приватна особа"`, бот підставляє справжній ПІБ людини.
  - Змінено класифікацію у `get_outgoing_waybills` та `get_incoming_waybills`: порівняння вхідних та вихідних посилок здійснюється строго **за номером телефону (останні 9 цифр)** та GUID контрагента, виключаючи неточний збіг за ПІБ.
  - Додано команду ручної зміни ПІБ відправника: `/set_name ПІБ` (`/set_sender_name`).
- **Тестування**: додано новий тест `test_fetch_sender_profile_private_person_name_resolution` у `tests/test_nova_poshta.py`. Усі 24 тести виконано успішно (`24 passed`).

## [v0.15.0] - 2026-08-08
- **Повна ізоляція профілів користувачів Telegram та інтерактивне підтягування відправника**:
  - У `UserSettingsManager` (`src/storage.py`) змінено `get_effective_settings`: усунуто фолбек на адмінські ключі з `.env`. Нові неавторизовані користувачі мають порожній API-ключ і більше не бачать чужих посилок чи ТТН.
  - Реалізовано перевірку-захисник `ensure_user_configured`: якщо у користувача немає прив'язаного API-ключа, бот автоматично надсилає привітне покрокове керівництво з налаштування профілю.
  - У `NovaPoshtaClient` додано метод `fetch_sender_profile(api_key)`: при команді `/set_np_key` бот автоматично підтягує з Нової Пошти ПІБ відправника, телефон та GUID контрагента.
  - Додано нові інтерактивні команди налаштування профілю: `/set_city` (вибір міста відправника), `/set_warehouse` (вибір відділення/поштомату відправка), а також оновлено картку `/profile` / `/settings` / `⚙️ Налаштування`.
- **Тестування**: оновлено `tests/test_storage.py` для перевірки строгої ізоляції налаштувань. Усі 23 тести виконано успішно (`23 passed`).

## [v0.14.0] - 2026-08-08
- **Внутрішньосистемне 5-хвилинне кешування запитів ТТН (In-Memory Waybills Cache)**:
  - У `NovaPoshtaClient` додано глобальний словник кешу `_waybills_cache` із часом життя (TTL) **300 секунд (5 хвилин)**.
  - Реалізовано метод `_fetch_raw_waybills_with_cache`: при послідовному натисканні кнопок `📥 Вхідні (що їдуть)` та `📤 Вихідні (що їдуть)` повторний мережевий запит до API не виконується, а розпарсені дані віддаються миттєво з оперативної пам'яті.
  - Реалізовано метод `invalidate_waybills_cache`: при створенні нової ТТН (`create_waybill`) кеш автоматично скидається для негайного завантаження найсвіжіших даних.
- **Тестування**: додано новий тест `test_waybills_caching_behavior` у `tests/test_nova_poshta.py`. Усі 23 тести виконано успішно (`23 passed`).

## [v0.13.1] - 2026-08-08
- **Точне вибіркове фільтрування вихідних та вхідних посилок за профілем відправника/отримувача**:
  - У клієнті `NovaPoshtaClient` додано допоміжні функції нормалізації телефонів `_clean_phone` (порівняння за останніми 9 цифрами) та зіставлення ПІБ `_name_matches`.
  - Оновлено `get_outgoing_waybills` та `get_incoming_waybills`: тепер методи порівнюють дані відправника та отримувача з профілем користувача (`sender_phone`, `sender_name`, `sender_counterparty_ref`).
  - У `📤 Вихідні (що їдуть)` показуються лише ті ТТН, де користувач є Відправником.
  - У `📥 Вхідні (що їдуть)` показуються лише ті ТТН, де користувач є Отримувачем (або відправником є інша особа).
- **Тестування**: додано новий інтеграційний тест `test_outgoing_vs_incoming_separation` у `tests/test_nova_poshta.py`. Усі 22 тести виконано успішно (`22 passed`).

## [v0.13.0] - 2026-08-08
- **Розділення на Вихідні та Вхідні посилки з відносними датами доставки**:
  - Розділено меню посилок на дві окремі кнопки у постійній клавіатурі Telegram: `📤 Вихідні (що їдуть)` та `📥 Вхідні (що їдуть)`.
  - Реалізовано метод `get_incoming_waybills` в `NovaPoshtaClient` та фільтрацію забраних/отриманих посилок ("Отримано" / "Забрано" / status codes 9, 10, 11, 106).
  - Створено допоміжний форматер дат `format_relative_delivery_date`: для вхідних та вихідних посилок дати відображаються відносно поточного дня (*"Сьогодні о HH:MM"*, *"Завтра о HH:MM"*, *"Післязавтра"*, або *"ДД.ММ.РРРР"*).
- **Тестування**: додано нові тести у `tests/test_nova_poshta.py`. Усі 22 тести виконано успішно (`22 passed`).

## [v0.12.7] - 2026-08-08
- **Виправлено помилку `MESSAGE_TOO_LONG` у розділі `📦 Активні посилки` (`/parcels`)**:
  - Усунуто об'єднання всіх активних посилок в одне наддовге повідомлення, яке перевищувало ліміт Telegram (4096 символів).
  - Тепер кожна активна посилка виводиться окремою інформативною карткою з посиланням на відстеження, що виключає помилки розриву тексту Telegram API.

## [v0.12.6] - 2026-08-08
- **Фільтрація та очищення реєстрів (ScanSheet) за датою (до 2 днів) та статусом відправлення**:
  - У `NovaPoshtaClient.get_scan_sheets` період запиту змінено на 2 дні (`days_back=2`), та додано розпізнавання прапора друку/передачі `Printed` / `IsPrinted` (`ScanSheetInfo.is_printed`).
  - Оновлено обробник `cmd_registers` (`/registers` / `📋 Реєстри (ScanSheet)`): бот відображає лише актуальні невідправлені реєстри за останні 2 дні. Реєстри, старші за 2 дні або посилки у яких вже відправлені, відсіюються.
  - Реалізовано метод `purge_old_or_sent_scansheets` в `UserSettingsManager` (`src/storage.py`) для автоматичного очищення `user_scansheets.json`.
- **Тестування**: додано нові тести у `tests/test_scan_sheet.py`. Усі 20 тестів виконано успішно (`20 passed`).

## [v0.12.5] - 2026-08-08
- **Автоматичне відсіювання та вилучення відправлених ТТН із Чернеток (Live Shipped Waybills Filter & Purge)**:
  - Додано метод `get_documents_status` в `NovaPoshtaClient` (`TrackingDocument/getStatusDocuments`), який отримує актуальні статуси накладних з Nova Poshta API.
  - Оновлено обробник `cmd_drafts` (`/drafts` / `📝 Мої чернетки (ТТН)`): перед відображенням накладних бот перевіряє їхні статуси через Nova Poshta API. ТТН зі статусами відправлення або видалення ("Відправлено", "В дорозі", "Отримано" тощо) автоматично вилучаються з `user_drafts.json` і більше не відображаються у чернетках.
  - Додано метод `purge_sent_drafts` у `UserSettingsManager` (`src/storage.py`) для очищення застарілих чернеток з локальної бази.
- **Тестування**: додано нові модульні тести у `tests/test_nova_poshta.py` та `tests/test_storage.py`. Усі 18 тестів виконано успішно (`18 passed`).

## [v0.12.4] - 2026-08-07
- **Розширений пошук та парсинг реєстрів з API Нової Пошти**:
  - У запиті `ScanSheet/getScanSheetList` додано аргументи `DateTimeFrom` та `DateTimeTo` (за останні 30 днів), що дозволяє підтягувати реєстри, створені через офіційний мобільний додаток або кабінет НП.
  - Додано універсальний парсинг полів кількості накладних у реєстрі (`CountPoint`, `CountOfDocuments`, `CountOfTTN`, `CountDocuments`).
- **Тестування**: Усі 16 модульних тестів виконано успішно (`16 passed in 2.10s`).

## [v0.12.3] - 2026-08-07
- **Виправлення NameError `clear_user_active_session`**:
  - Відновлено допоміжну функцію `clear_user_active_session(user_id: int)` на рівні модуля `src/bot/handlers.py`.

## [v0.12.2] - 2026-08-07
- **Виправлення NameError `storage_manager` при асинхронній обробці накопичених повідомлень**.

## [v0.12.1] - 2026-08-07
- **Виправлення обробки команди створення реєстру з усіх чернеток (`is_register_intent`)**.
- **Фільтрація порожніх/відправлених реєстрів (`count_of_documents > 0`)**.

## [v0.12.0] - 2026-08-07
- **Реєстри експрес-накладних (ScanSheet) та фільтрація чернеток живою мовою**.
- **Генерація штрих-кодів Code128 у Telegram**.
- **Уточнення адресної кур'єрської доставки додому (`AddressConfirmCallback`)**.

## [v0.11.0] - 2026-08-07
- **Усунення зайвих повторних API-запитів при локальних оновленнях полів (Preserve Resolved Location on Field Edits)**.

## [v0.10.1] - 2026-08-07
- **Обробка обмежень частоти запитів Nova Poshta API (`To many requests`)**.

## [v0.10.0] - 2026-08-07
- **Усунення дублювання назв населених пунктів (Duplicate City Disambiguation & Cross-City Branch Lookup)**.

## [v0.9.0] - 2026-08-07
- **Асинхронний дибаунсинг та буферизація перепощених повідомлень (Async Message Debouncer)**.

## [v0.8.1] - 2026-08-07
- **Виправлено помилку Pydantic `ValidationError` при об'єднанні повідомлень**.

## [v0.8.0] - 2026-08-07
- **Повна відмова від регулярних виразів (Pure AI-Driven Extraction)**.

## [v0.7.2] - 2026-08-07
- **Виправлено парсинг номера відділення/поштомату з опису адреси**.
- **Скидання активного контексту сесії (Session Context Cleanup)**.

## [v0.7.1] - 2026-08-07
- **Виправлено помилку `NameError: ParsedRecipientInfo`**.

## [v0.7.0] - 2026-08-07
- **Накопичення та об'єднання перепощених/багатокомпонентних повідомлень (Multi-Part Message Accumulation)**.

## [v0.6.0] - 2026-08-07
- **Виправлено помилку `NameError: WaybillItemInfo`**.
- **Редагування чернеток ТТН живою мовою (Draft Live Editing)** через `InternetDocument/update`.

## [v0.5.0] - 2026-08-07
- **Контекстна пам'ять та доопрацювання активної ТТН (Context Awareness)**.
- **Інтерфейс кнопками (Reply Keyboard Menu)**.
- **Повна українізація інтерфейсу (Ukrainian UI)**.

## [v0.4.1] - 2026-08-07
- **Виправлено сумісність з новітніми моделями OpenAI (`gpt-5.6-luna`, `o1`, `o3-mini`)**.

## [v0.4.0] - 2026-08-07
- **Керування створеними накладними/чернетками (`/drafts`)**.

## [v0.3.0] - 2026-08-07
- **Додано розпізнавання розмовного інтенту (Conversational Chat)**.

## [v0.2.0] - 2026-08-07
- **Додано команду `/parcels`**, перевірку обов'язкових полів та циклювання оціночної вартості.

## [v0.1.3] - 2026-08-07
- **Виправлено роботу з поштоматами** та додано `OptionsSeat`.

## [v0.1.2] - 2026-08-07
- Реалізовано базовий функціонал бота.
